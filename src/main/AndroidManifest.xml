<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pyhy"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity">
            <!-- 可以添加启动模式等属性 -->
        </activity>
        <activity android:name=".Login.User_Main"/>
        <activity android:name=".Login.UserInfoActivity"/>
        <activity android:name=".Login.TreeHoleActivity"/>
        <activity android:name=".Login.SmartChatActivity"/>
        <activity android:name=".Login.TreeHole.PostTreeHoleActivity"/>
        <activity android:name=".Login.PsychResourcesActivity"/>
        <activity android:name=".Login.MessagesActivity"/>
        <activity android:name=".Login.FriendsActivity"/>
        <activity android:name=".Login.ShortVideosActivity"/>

        <activity android:name=".Login.MoodRecordActivity"/>
    </application>

</manifest>